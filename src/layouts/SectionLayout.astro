---
const { id, isDark = false } = Astro.props;
---

<section id={id} class:list=
{[
	"full-width h-full py-10  sm:px-8 lg:px-16 xl:px-32",
	isDark ? "bg-[#191925] text-light" : "bg-light text-dark",
]}>
	<div class="relative max-w-[1200px] mx-auto p-4  z-10">
		<slot />
	</div>	
</section>